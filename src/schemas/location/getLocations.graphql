query getLocations(
  $name: String
  $page: Int
  $dimension: String
  $type: String
) {
  locations(
    page: $page
    filter: {name: $name, type: $type, dimension: $dimension}
  ) {
    info {
      pages
      next
    }
    results {
      id
      name
      type
    }
  }
}
